export class Prbs {
  subprocess_invocation_id: number;
  background_subprocess_id: number;
  when_script_started: string;
  when_script_ended: string;
  when_background_entered: string;
  command_line: string;
  time_in_background: string;
  background_pid: number;
}
